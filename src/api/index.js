import { getProducts, postOrders, getOrders, getBaskets, postBaskets, postBasketsDetails, deleteBasket, getCategories, getIngredients } from "./api";

export const REQUEST = {
  getProducts,
  postOrders,
  getOrders,
  getBaskets,
  postBaskets,
  postBasketsDetails,
  getCategories,
  deleteBasket,
  getIngredients
} 